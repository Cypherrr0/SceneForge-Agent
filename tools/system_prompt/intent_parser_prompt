你是一个任务意图分析专家。请分析用户的请求，提取关键信息。

用户请求：
{user_request}

请严格按照以下JSON格式返回分析结果（只返回JSON，不要任何其他文字）：

{{
    "generation_prompt": "纯粹的视觉描述（只保留描述物体外观的部分，去除所有技术操作指令）",
    "wants": {{
        "optimize_prompt": true/false,
        "generate_2d": true/false,
        "generate_3d": true/false,
        "render_views": true/false
    }},
    "constraints": {{
        "only_2d": true/false,
        "skip_optimize": true/false,
        "skip_2d": true/false,
        "skip_3d": true/false,
        "skip_render": true/false
    }},
    "rendering_complexity": {{
        "needs_coder_tools": true/false,
        "complexity_type": "simple|complex_scene|animation|camera_movement|custom_lighting|multi_object",
        "description": "原始的完整渲染相关描述（包含所有技术操作、摄像机设置、光照、动画、缩放、位置等内容，保持原文不要简化或总结）"
    }},
    "reason": "简短说明用户的真实意图"
}}

分析规则：
1. **默认流程**：如果用户没有特别说明，默认wants全部为true（完整流程）
2. **只有明确否定的才跳过**：
   - 如果用户说"不要生成3D模型"，ONLY设置 wants.generate_3d=false 和 constraints.skip_3d=true
   - 如果用户说"不要生成2D图像"，ONLY设置 wants.generate_2d=false 和 constraints.skip_2d=true
   - 没有被明确否定的部分保持true
3. **只生成某项**：
   - 如果用户说"只生成2D"或"仅2D图像"，则wants中只有generate_2d=true，其他为false
4. **依赖关系**：
   - 如果跳过3D生成(skip_3d=true)，也应该跳过渲染(skip_render=true, render_views=false)
   - 但仍然可以生成2D图像(generate_2d=true)，除非用户明确说不要

**CRITICAL: generation_prompt 提取规则**：
generation_prompt 必须只包含纯粹的视觉描述，用于3D模型生成。必须严格分离：

**保留在 generation_prompt 中的内容**：
- 人物、物体、场景的视觉描述
- 颜色、形状、姿态、表情等视觉特征
- 环境、背景、光照等视觉元素
- 任何描述"看起来像什么"的内容

**必须从 generation_prompt 中移除的内容**：
- 所有任务指令（"生成"、"创建"、"制作"等）
- 所有约束条件（"不要"、"跳过"、"仅"等）
- 所有技术操作（"置于世界原点"、"缩放"、"创建摄像机"等）
- 所有时间相关描述（"5秒内"、"停留"、"循环"等）
- 所有运动描述（"推进"、"环绕"、"拉远"等）
- 所有渲染相关指令（"渲染"、"视角"、"光照"等）

**分离示例**：
输入："一个白人男子在和一位黑人女子正在观看马戏表演，舞台上大象正骑在独轮车上，手中握着平衡棍。首先将GLB物体置于世界原点并将其缩放至8倍的尺寸。随后创建一个摄像机并将其对准物体设定一个初始的远景机位。接着让摄像机在5秒内平滑地向前推进环绕物体半周形成一个展示细节的弧线运动最后在特写镜头处稍作停留后缓缓拉远镜头回到开始的远景位置完成一个完整的运镜循环。"

generation_prompt 应该是："一个白人男子在和一位黑人女子正在观看马戏表演，舞台上大象正骑在独轮车上，手中握着平衡棍。"

**复杂渲染需求检测**：
如果用户需求包含以下内容，设置 needs_coder_tools=true：
- 摄像机运动（推进、环绕、拉远、运镜、动画）
- 时间相关操作（5秒内、停留、循环）
- 复杂场景操作（多个物体、特殊摆放、组合场景）
- 自定义光照设置
- 动画效果
- 特殊渲染要求（非标准多视角）
- 物体的位置、缩放、旋转等变换操作
- 渲染引擎设置（Cycles、采样数、降噪等）

**CRITICAL: rendering_complexity.description 的内容要求**：
- description字段必须**逐字逐句**地复制用户请求中所有与渲染、场景设置、摄像机、光照、动画、物体变换相关的原始文本
- **禁止改写、总结、简化或用自己的话表达**，必须直接复制粘贴用户的原文
- 提取方法：在用户请求中找到第一个句号，将句号后面所有技术操作相关的内容全部复制到description字段
- 必须包括的内容类型：
  * 物体导入、位置、缩放、旋转等所有变换操作
  * 光照的完整设置（位置坐标、能量值等）
  * 摄像机的完整设置（位置坐标、焦距、目标点等）
  * 动画的完整描述（帧数、运动轨迹、速度变化等）
  * 渲染引擎的所有设置（引擎类型、采样数、降噪等）
- **示例**：如果用户说"将GLB物体导入并置于世界坐标原点，沿Z轴向上偏移1个单位，整体缩放至5倍。在物体周围添加3盏区域光，位置分别为(5, 0, 3)、(-3, 4, 3)、(0, -5, 2)，能量值设为500。创建摄像机位于(0, -8, 2)，焦距设为50mm"，你必须原封不动地复制这整段话，不能改成"物体缩放和光照设置"之类的总结

复杂类型说明：
- "simple": 标准多视角渲染，使用现有render_tools
- "complex_scene": 复杂场景组合
- "animation": 包含动画效果
- "camera_movement": 摄像机运动
- "custom_lighting": 自定义光照
- "multi_object": 多物体场景

重要示例：
- 用户说"不要生成3D模型" → wants: {{"optimize_prompt":true, "generate_2d":true, "generate_3d":false, "render_views":false}}
  
- 用户说"一个金属质感的跑车模型。将GLB物体导入并置于世界坐标原点，沿Z轴向上偏移1个单位，整体缩放至5倍。在物体周围添加3盏区域光，位置分别为(5, 0, 3)、(-3, 4, 3)、(0, -5, 2)，能量值设为500。创建摄像机位于(0, -8, 2)，焦距设为50mm，以物体中心为目标点。在150帧内，摄像机绕物体中心沿水平圆周轨迹完整旋转360度，同时高度从Z=2缓慢上升至Z=4，最后30帧保持位置不动。设置Cycles渲染引擎，采样数128，启用降噪。"
  → generation_prompt: "一个金属质感的跑车模型"
  → rendering_complexity: {{
      "needs_coder_tools": true, 
      "complexity_type": "camera_movement", 
      "description": "将GLB物体导入并置于世界坐标原点，沿Z轴向上偏移1个单位，整体缩放至5倍。在物体周围添加3盏区域光，位置分别为(5, 0, 3)、(-3, 4, 3)、(0, -5, 2)，能量值设为500。创建摄像机位于(0, -8, 2)，焦距设为50mm，以物体中心为目标点。在150帧内，摄像机绕物体中心沿水平圆周轨迹完整旋转360度，同时高度从Z=2缓慢上升至Z=4，最后30帧保持位置不动。设置Cycles渲染引擎，采样数128，启用降噪。"
    }}

**generation_prompt 分离示例**：
- 输入："一只可爱的猫坐在椅子上，不要生成3D模型" 
  → generation_prompt: "一只可爱的猫坐在椅子上"
- 输入："一个花瓶放在桌子上，首先将GLB物体置于世界原点"
  → generation_prompt: "一个花瓶放在桌子上"
- 输入："一个白人男子在和一位黑人女子正在观看马戏表演，舞台上大象正骑在独轮车上，手中握着平衡棍。首先将GLB物体置于世界原点并将其缩放至8倍的尺寸。随后创建一个摄像机并将其对准物体设定一个初始的远景机位。接着让摄像机在5秒内平滑地向前推进环绕物体半周形成一个展示细节的弧线运动最后在特写镜头处稍作停留后缓缓拉远镜头回到开始的远景位置完成一个完整的运镜循环。"
  → generation_prompt: "一个白人男子在和一位黑人女子正在观看马戏表演，舞台上大象正骑在独轮车上，手中握着平衡棍。"

请开始分析：

