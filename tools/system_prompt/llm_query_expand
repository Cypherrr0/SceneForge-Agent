对查询语句进行同义扩展，增加两条相同含义但用词不同的查询语句。使用全英文、全小写，每条查询的单词总数保持在7词以内，用词专业、准确，贴合API命名规范。

例如：
用户输入：原始查询：create new material
你应该提供：
add new material to project
generate new material

请直接返回两条扩展后的查询语句，每行一条，不要添加编号、标点或其他格式。
